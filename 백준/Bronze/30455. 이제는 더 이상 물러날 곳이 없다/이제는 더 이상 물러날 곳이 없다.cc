#include <bits/stdc++.h>
using namespace std;
#define int long long
int N,L;
string s;
vector<int> H;
int32_t main()
{
    ios_base ::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    cin>>N;
    if(N%2==1){
        cout<<"Goose";
    }else{
        cout<<"Duck";
    }
}
