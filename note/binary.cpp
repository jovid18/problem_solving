#include <bits/stdc++.h>
using namespace std;
#define int long long
#define pii pair<int, int>
int32_t main() {
    cin.tie(0)->sync_with_stdio(0);
    vector<int> v;
    // chk[lo] != chk[hi]
    // while(lo + 1 < hi) -> chk[mid] == chk[lo] ? lo = mid : hi = mid;
}