function solution(sizes) {
  // 최소 직사각형
  // sizes : [[60, 50], [30, 70], [60, 30], [80, 40]]
  // result : 4000

  // 더 큰 길이를 가로로 두기
  for (let i = 0; i < sizes[].length; i++) {}

  let answer = 0;
  return answer;
}
